export class Candidate {
    // Declare a private variable named votes
    // This variable should be a number representing the number of votes the candidate has received


    // Create a constructor that initializes the candidate with a name and party
    // The constructor should take two parameters: name (string) and party (string)
    // The constructor should set the candidate's name and party based on the parameters


    // Create a method named getDetails that returns a string with the candidate's name and party
    // The method should return the name and party in the following format: "[name] ([party])"


    // Create a method named addVote that increments the candidate's vote count by 1


    // Create a method named getVotes that returns the candidate's vote count


    // Create a method named resetVotes that sets the candidate's vote count to 0

}