import { Candidate } from './Candidate';
import { VotingMachine, Winner } from './VotingMachine';

export function run() {

// Step 1: Create instances of Candidate for the race
// Create three candidates with the following details:
// Alice - Party A
// Bob - Party B
// Charlie - Party C


// Step 2: Create an instance of VotingMachine

// Step 3: Add a race and candidates to the voting machine

// Log out the candidates by calling the getCandidatesByRace method
// and then iterate over the candidates, calling getDetails on each one and logging the result


// Step 4: Simulate voting for the race

// Step 5: Determine and display the winner for the race
// Call the determineWinner method on the voting machine and log out the winner's details
// Use this format: "The winner of the [raceName] race is: [winner details] with [number of votes] votes."

}

// If this file is being run directly, call the run function
run();